# Quote Agent 后端依赖（Python 3.11+）
# 安装：pip install -r requirements.txt
fastapi>=0.115
uvicorn>=0.30
langgraph>=0.2.0
langchain-openai>=0.2.0
python-dotenv>=1.0
langgraph-checkpoint-sqlite>=2.0.0
hnswlib>=0.8.0

# 桌面版：python main.py 用的原生窗口外壳。
# 未安装（或无 GUI 后端）时主程序会自动回落到默认浏览器，仍可正常使用。
pywebview>=5.0

# 打包成单文件可执行程序：python build.py
# 注意 PyInstaller 不支持交叉编译，Windows 的 .exe 需在 Windows 上执行 build.py
pyinstaller>=6.0
